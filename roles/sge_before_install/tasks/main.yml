---
  - name: configuration the /etc/hosts file
    lineinfile:
      path: /etc/hosts
      regexp: "^{{item.ip}}"
      line: "{{item.ip}} {{item.hostname}}"
    with_items:
      - "{{ etc_hosts }}"
    tags:
      - step1

  
